%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%
% Command Parser
% Takes human input (i.e. "Attack troll with sword")
% and parses it into a game-usable format.
%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

-module(parser).
-export( [ parse/1 ] ).

parse(String) ->
	String.
